---
import Footer from "../components/layout/Footer.astro";
import Head from "../components/layout/Head.astro";
import Header from "../components/layout/Header.astro";
import SocialStickyBar from "../components/SocialStickyBar.astro";
import SideMenu from "../components/layout/SideMenu.astro";
import "../styles/global.css";

const {
  title = "Sai International School",
  description = "Join Sai International School",
  ogImage = "",
  ogUrl = "",
} = Astro.props;
---

<html lang="en">
  <head>
    <meta charset="utf-8" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />

    <!-- Metadata -->
    <Head
      title={title}
      description={description}
      ogImage={ogImage}
      ogUrl={ogUrl}
    />
    <meta name="viewport" content="width=device-width" />
    <meta name="generator" content={Astro.generator} />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Lora:ital,wght@0,400..700;1,400..700&family=Poppins:ital,wght@0,100;0,200;0,300;0,400;0,500;0,600;0,700;0,800;0,900;1,100;1,200;1,300;1,400;1,500;1,600;1,700;1,800;1,900&display=swap"
      rel="stylesheet"
    />
    <link
      href="https://fonts.googleapis.com/css2?family=Poppins:wght@100;200;300;400;500;600;700;800&display=swap"
      rel="stylesheet"
    />
    <title>{title}</title>
  </head>
  <body>
    <main>
      <Header />
      <SideMenu />
      <slot />
      <!-- Footer -->
      <Footer />
    </main>
    <SocialStickyBar />
  </body><script>
    const observer = new IntersectionObserver(
      (entries, obs) => {
        entries.forEach((entry) => {
          if (entry.isIntersecting) {
            entry.target.classList.add("in-view");
          }
        });
      },
      { threshold: 0.2 } // reveal when 20% visible
    );

    document.addEventListener("DOMContentLoaded", () => {
      document
        .querySelectorAll(".fade-in, .scale-in, .card-anim, .fade-in-down")
        .forEach((el) => {
          observer.observe(el);
        });
    });
  </script>
  <script src="https://unpkg.com/embla-carousel/embla-carousel.umd.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/axios/dist/axios.min.js"></script>
</html>
