---
/**
 * Generate static paths for all albums
 * @returns Array of static paths with album data
 */
export async function getStaticPaths() {
  const paths: Array<{
    params: { albumId: string };
    props: {
      albumId: string;
    };
  }> = [];

  return paths;
}
---
