---
// COMPONENTS //
import Article from "@/components/ui/Article.astro";
import Breadcrumbs from "@/components/ui/Breadcrumbs.astro";

// CONSTANTS //
import { URLS } from "@/infrastructure/constants/urls";

// OTHERS //
import Layout from "@/layouts/Layout.astro";

// META //
import globalConnectMeta from "@/data/meta/global-connect/global-connect.json";

// DATA //
import {
  advancedPlacementsContent,
  sectionHeaderContent,
  metaData,
} from "@/data/global-connect/advanced-placements";
import Image from "astro/components/Image.astro";

// Prepare breadcrumb items
const breadcrumbItems = [
  { label: "Global Connect", href: URLS.GLOBAL_CONNECT.ROOT },
  { label: "Advanced Placements", href: `#` },
];
---

<Layout meta={globalConnectMeta}>
  <section class="bg-n-50 relative w-full overflow-hidden">
    <div
      class="container mx-auto flex flex-col gap-5 px-6 pt-34 pb-30 md:gap-8 md:px-8 md:pt-40 md:pb-40 lg:gap-9 2xl:px-0"
    >
      <Breadcrumbs items={breadcrumbItems} />
      <!-- Advanced Placements Section -->
      <Article
        title={sectionHeaderContent?.title}
        subtitle={sectionHeaderContent?.subtitle}
        metaStripDetails={metaData}
      >
        <!--  Advanced Placements Content -->
        <div class="flex flex-col gap-10 md:gap-14 lg:gap-20">
          <!-- Advanced Placements Image and Details -->
          {
            advancedPlacementsContent.landscapeImg && (
              <Image
                src={advancedPlacementsContent.landscapeImg}
                alt=" Advanced Placements Image"
                class="card-anim card-bottom h-[257px] w-full rounded-3xl object-cover md:h-[524px] lg:h-[600px]"
                style={`--delay: 0.1s`}
                width={1200}
                height={600}
              />
            )
          }

          <!--  Advanced Placements Description -->
          <div set:html={advancedPlacementsContent.content} />
        </div>
      </Article>
    </div>
  </section>
</Layout>
